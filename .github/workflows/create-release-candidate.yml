name: "Create Release Candidate"
on:
  deployment_status:

jobs:
  create:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Env ${{ github.deployment_status.environment }}"
      - run: |
          echo "Status ${{ github.deployment_status.state }}"
      - run: |
          echo "Github ${{ toJson(github) }}"
