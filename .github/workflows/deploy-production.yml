name: Deploy Production
on:
  release:
    types:
      - released
concurrency:
  group: production

jobs:
  deploy:
    - uses: ./.github/workflows/deploy
      with:
        env: production
        version: "${{env.version }}"
