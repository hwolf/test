name: Deploy Production
on:
  release:
    types:
      - released
concurrency:
  group: production

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: ./.github/actions/deploy
      with:
        env: production
        version: "${{env.version }}"
