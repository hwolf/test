name: Build
description: 'Builds the project'
runs:
  using: composite
  steps:
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        #cache: maven
    - name: Build
      shell: bash
      run: echo "Verify ..." && java --version && ls -l && cat .m2/settings.xml
        #- mvn --batch-mode --update-snapshots verify
